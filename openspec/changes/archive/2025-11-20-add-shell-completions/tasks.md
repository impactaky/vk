# Tasks

## Implementation

- [x] Import CompletionsCommand from @cliffy/command
- [x] Register completions command in main.ts
- [x] Run deno fmt and deno lint
- [x] Run deno check for type validation

## Validation

- [x] Test `vk completions bash` outputs valid bash script
- [x] Test `vk completions zsh` outputs valid zsh script
- [x] Test `vk completions fish` outputs valid fish script
