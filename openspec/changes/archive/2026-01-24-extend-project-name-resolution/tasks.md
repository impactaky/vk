## 1. Implementation
- [x] 1.1 Update `show` command to use `getProjectId()` for name resolution
- [x] 1.2 Update `update` command to use `getProjectId()` for name resolution
- [x] 1.3 Update `delete` command to use `getProjectId()` for name resolution
- [x] 1.4 Update `repos` command to use `getProjectId()` for name resolution
- [x] 1.5 Update `add-repo` command to use `getProjectId()` for name resolution
- [x] 1.6 Update `remove-repo` command to use `getProjectId()` for name resolution

## 2. Testing
- [x] 2.1 Verify existing project resolver integration tests pass
- [x] 2.2 Verify type checking passes (`deno task check`)
