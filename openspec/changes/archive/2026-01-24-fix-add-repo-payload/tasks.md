## 1. Implementation

- [x] 1.1 Update `CreateProjectRepo` type in `src/api/types.ts` to use `display_name` and `git_repo_path`
- [x] 1.2 Update `addProjectRepo` method in `src/api/client.ts` to use correct API contract
- [x] 1.3 Update `add-repo` command in `src/commands/project.ts` to use `--path` and `--display-name` options
- [x] 1.4 Update `project create` command to use `--repo-path` and `--repo-name` options
- [x] 1.5 Add integration tests for add-repo API in `tests/api_integration_test.ts`
- [x] 1.6 Run lint and type checks to verify changes compile correctly
- [x] 1.7 Run tests to verify functionality works correctly
