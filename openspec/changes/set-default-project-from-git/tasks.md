# Tasks

## Implementation Tasks

1. [ ] Create git utility to extract remote URL basename from current directory
2. [ ] Create project lookup service to find project by git URL basename
3. [ ] Integrate default project resolution into CLI command handler
4. [ ] Add tests for git URL parsing (various formats: https, ssh, with/without .git)
5. [ ] Add tests for project lookup by basename
6. [ ] Add integration test for default project resolution flow
