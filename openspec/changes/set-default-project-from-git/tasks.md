# Tasks

## Implementation Tasks

- [x] Create git utility module with functions to get remote URL and extract basename
- [x] Create project resolver utility that matches current git repo to a project
- [x] Update task list command to use project resolver when --project is omitted
- [x] Update task create command to use project resolver when --project is omitted
- [x] Add tests for git utility functions
- [x] Add tests for project resolver
- [x] Run deno check, deno lint, deno fmt to validate changes
