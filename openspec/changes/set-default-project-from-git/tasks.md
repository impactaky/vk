# Tasks

## Implementation Tasks

- [ ] Create git utility module with functions to get remote URL and extract basename
- [ ] Create project resolver utility that matches current git repo to a project
- [ ] Update task list command to use project resolver when --project is omitted
- [ ] Update task create command to use project resolver when --project is omitted
- [ ] Add tests for git utility functions
- [ ] Add tests for project resolver
- [ ] Run deno check, deno lint, deno fmt to validate changes
