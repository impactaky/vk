# Tasks

## Implementation

- [ ] Import CompletionsCommand from @cliffy/command
- [ ] Register completions command in main.ts
- [ ] Add tests for completions command registration
- [ ] Run deno fmt and deno lint
- [ ] Run deno check for type validation

## Validation

- [ ] Test `vk completions bash` outputs valid bash script
- [ ] Test `vk completions zsh` outputs valid zsh script
- [ ] Test `vk completions fish` outputs valid fish script
