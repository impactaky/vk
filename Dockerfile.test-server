FROM node:20-slim

RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*

# Pre-download vibe-kanban binary
RUN npx vibe-kanban --version || true

EXPOSE 3000

ENV PORT=3000
ENV HOST=0.0.0.0

CMD ["npx", "vibe-kanban"]
